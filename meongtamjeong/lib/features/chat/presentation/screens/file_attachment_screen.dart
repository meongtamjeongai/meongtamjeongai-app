import 'package:flutter/material.dart';

class FileAttachmentScreen extends StatelessWidget {
  const FileAttachmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('파일 선택 기능은 채팅창에서 사용됩니다.')));
  }
}
