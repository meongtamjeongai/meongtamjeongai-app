import 'package:flutter/material.dart';

class ImageAttachmentScreen extends StatelessWidget {
  const ImageAttachmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('이미지 선택 기능은 채팅창에서 사용됩니다.')));
  }
}
